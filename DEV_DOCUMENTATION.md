
Det meste af koden ligger i frontenden. `Board` repræsenterer et spil-board.


## How-to til fremtidige udviklere

### For at ændre på AI-opførslen, ændre `Ai`-klassen i `frontend/src/board.ts`.

## Setup til fremtidige udviklere

Installer dependencies i frontend ved at installere NodeJS og kør `npm install`. Kør `npm run dev` til at køre i development og `npm run build` for at bygge til production.




